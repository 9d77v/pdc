export interface IBookPosition {
    bookshelf_id: number,
    layer: number,
    partition: number
}

export interface IUpdateBookPosition {
    id: number,
    layer: number,
    partition: number
}

